{"scene": "用MT管理器定位打开最近生成的技能 zip 包", "path": "① am start ... -n bin.mt.plus/bin.mt.plus.OpenFileActivity   # 定位\n② uiautomator dump → 找到文件名 item 中心 → input tap    # 弹出「打开方式」\n③ dump 找「文本编辑器」→ tap；若未进再 dump 找「打开」→ tap\n④ 点右上「≡」→ dump 找「跳转到指定行」→ tap\n⑤ 点输入框 → keyevent 67 清默认 → input text '目标行' → 点「确定」", "pit": "① 必须 `OpenFileActivity`，勿用 `.Main`；② 跳行输入框**先清空默认值**否则变 `1241`；③ 每次点击前先 dump 拿实时坐标，勿复用旧坐标。", "reuse": "高", "strategy": "auto"}
{"scene": "Operit 对话中拉起其他 App（如 MT/Chrome）", "path": "", "pit": "**Operit 会抢占前台**：`am start` 拉起目标 App 后，Operit 的 MainActivity 可能秒切回前台，导致界面跳动/操作被打断。对策：拉起后**立刻**（sleep≤1）screencap 确认；被抢回后**勿反复 am start**（会切断已打开的窗口），退化为引导用户手动点一步。", "reuse": "高", "strategy": "auto"}
{"scene": "系统输入法注入英文/符号", "path": "settings put secure default_input_method com.android.inputmethod.latin/.LatinIME   # 切AOSP英文键盘\ninput text 'sk-xxx...' / '<符号>'                                                # 注入\nsettings put secure default_input_method com.iflytek.inputmethod/.FlyIME          # 用完切回讯飞", "pit": "系统默认讯飞会把英文当拼音候选变乱码；注入前必须切 AOSP 键盘。仅注入纯数字（如行号）可不必切。", "reuse": "高", "strategy": "auto"}
{"scene": "技能升级-版本清理纪律", "path": "每次升级某个Skill：①先删除旧版（旧目录/旧zip/带旧版本号的副本）\n②再放置新的：单一源码目录 <skill>/SKILL.md + 单一导入包 <skill>-skill.zip\n③命名为固定名（不累积多版本），zip与目录同名同步更新\n④如需版本号，用 -v数字 但更新时先删旧的再放新的", "pit": "旧版残留会被Operit扫描到造成重复/冲突；升级必须：先删旧→再放新，只留最新一份", "reuse": "高", "strategy": "auto"}
{"scene": "技能升级-就地优先", "path": "日常升级某技能：直接在Operit生效目录 Download/Operit/skills/<skill>/SKILL.md 上编辑即可，无需打包zip、无需重新导入(Operit直接从该目录读)。\n流程：①就地编辑生效目录SKILL.md→保存即升级 ②同步源码备份 我的Skill/<skill>/SKILL.md ③若有缓存重启Operit一次 ④zip仅在首次导入他设备/分享/数据重置后才需要", "pit": "以前每次升级都打zip是多余动作；除非要分发/导入他设备，否则不必生成zip", "reuse": "高"}
{"scene": "X浏览器打开本地HTML后无关离线弹窗关闭", "path": "X浏览器打开本地html后底部会弹‘发现了可读的离线文件，是否前往查看？’无关弹窗，必须点右上角 × (content-desc=close 的 ImageView) 关闭，不要点‘查看’。关闭按钮bounds定位: content-desc='close' clickable=true class=ImageView", "pit": "无关弹窗必须关闭; 关闭键是右上角close(×)不是查看; 弹窗若不关会遮挡底部导航/内容", "reuse": "高", "strategy": "auto"}
{"scene": "MT分享→X浏览器打开HTML并定位页面锚点", "path": "长按文件(315,953)弹菜单→点分享(810,1322)→选X浏览器(404,1980)→关闭离线弹窗(1018,2088)→点导航核心能力(337,231)", "pit": "坐标随设备变化需每次dump动态取; OpenFileActivity定位后Operit可能抢前台需重新调起", "reuse": "高", "strategy": "auto"}
{"scene": "安卓通用-Bottom手势返回桌面(随机短促上滑)", "path": "场景: 需要从任意App退回到桌面(全面屏手势导航,绝大多数安卓通用)。路径: input swipe SX SY EX EY D 其中 SX=底部中间区域(如540±60小偏差) SY=底部边缘(如2335~2350) EX,SY附近 EX=底部中间±60 EY=上滑一小段(约2040~2170,距起点约200~300px) D=时长80~150ms(快速短促)。起点始终贴近底部中间,偏差小相差不多;滑动距离短促;每次随机化模拟人手自然差异。", "pit": "关键: 必须短促+快速+距离短,不要划到屏幕中部;起点固定在底部中间±小偏差;不要用固定直线坐标;模拟人手随机性;上滑方向不变", "reuse": "中", "strategy": "auto"}
